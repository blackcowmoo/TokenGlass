## 1. Tauri 백엔드 윈도우/트레이 설정

- [x] 1.1 `tauri.conf.json` 수정하여 기본 창 숨김 및 프레임리스 설정
- [x] 1.2 `src-tauri/src/main.rs` 에 `tauri-plugin-tray` (혹은 동급 플러그인) 초기화 코드 추가
- [x] 1.3 트레이 좌클릭 이벤트 핸들러 구현 (메인 윈도우 토글 및 위치 계산)
- [x] 1.4 창이 포커스를 잃었을 때(`tauri::WindowEvent::Focused(false)`) 창 숨기기 구현

## 2. 프론트엔드 라우팅 및 UI 대응

- [x] 2.1 React(Vite) 진입점(App.tsx 등)에 라우터 추가 (메인 팝업 `/`, 위젯 `/widget`)
- [x] 2.2 메인 팝업 창에 맞게 CSS 수정 (둥근 테두리, 레이아웃 리사이징)
- [x] 2.3 위젯용 초소형 컴포넌트(`Widget.tsx`) 신규 개발 (투명 배경, 필수 정보만 표시)

## 3. 위젯 기능 연동

- [x] 3.1 트레이 우클릭 컨텍스트 메뉴에 "위젯 켜기/끄기" 토글 메뉴 추가
- [x] 3.2 토글 상태에 따라 Rust 백엔드에서 위젯 전용 Window(`Widget Window`) 동적 생성 및 소멸 로직 작성
- [x] 3.3 위젯 드래그 이동 기능 활성화 (`data-tauri-drag-region` 속성 사용 등)
- [x] 3.4 앱 시작 시 위젯 On/Off 설정을 읽어 자동으로 창 띄우기 연동
